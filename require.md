# DepScaner：大型 C++ 项目依赖关系分析与可视化 VS Code 插件（开发提示词）
## 角色设定
你是一名资深的全栈 C++ / TypeScript 工程师，同时精通 VS Code 扩展开发、编译原理（基于 Clang/LLVM 与 tree‑sitter）以及依赖图可视化架构。你能够根据下面这份完整需求，从零生成一个可编译、可运行、可发布的 VS Code 插件工程。

## 一、项目概述
开发一个 VS Code 插件（Extension），面向 C/C++开发者，用于对大型 C++ 项目进行多种依赖关系的识别与分析，帮助开发者理解、学习和浏览项目结构。功能明细见第三章，开箱即用且支持百万级代码行与增量索引。

### 0.1 插件命名与界面语言
- 插件统一命名为 **DepScaner**（即 Dependency Scan 的缩写）。
- 请在所有代码、命令、配置项、图标标注、`package.json`（name、displayName、publisher）、目录名、文档标题中保持一致，统一使用 DepScaner 作为正式名称。
- UI 文案中英双语，由 `locals/i18n` 机制切换，默认中文；教学文档为中文。

## 二、技术栈与架构要求
### 2.1 总体架构（分层）
采用「C++ 高性能分析引擎 + TypeScript 插件外壳」的的两层架构：

> VS Code 插件层（TypeScript / node）
> - 命令注册、状态栏、菜单、配置项
> - Webview 依赖图 UI（力导向图 / 树 / 列表）
> - 与引擎进程通信（JSON‑RPC / stdio / 端口）
> - 增量索引调度、磁盘缓存管理
> ↕️ IPC
> C++ 分析引擎（独立进程 / 可执行文件）
> - 语法/语义解析（Clang + fallback tree‑sitter）
> - 建立符号表、调用图、include 图、类型/继承图
> - 百万级 LOC 多线程并行、增量构建 AST/图
> - 结果序列化输出（JSON / 二进制协议）

### 2.2 语言与构建
- 分析引擎：C++20，使用 libclang（clang‑c API）进行精确语义解析。
- 解析 fallback：使用 tree‑sitter（C/C++ grammar）进行轻量、免配置的快速解析，作为无 `compile_commands.json` 时的备用方案。
- 插件外壳：TypeScript + VS Code Extension API；Webview 前端使用 d3‑force + Canvas 渲染依赖图。
- 通信：插件与 C++ 引擎间统一使用 stdio JSON‑RPC（不采用 node‑addon / 本地端口，避免跨平台差异）。
- 构建：C++ 用 CMake；TS 用 esbuild 打包；为 Windows / Linux / macOS 三平台交叉编译引擎二进制。

### 2.3 多平台与引擎交付
- 必须支持 Windows、Linux、macOS 三平台；引擎二进制在 `package.json` 内按 os/cpu 分发，插件运行时自动选择。
- 交付方式：内置编译解析引擎。vsix 直接携带三平台预编译的可执行引擎，用户（插件使用者）的开发机无需安装任何编译器/Clang，下载即用；文档中注明安装包体积权衡。
- 提供三平台的编译脚本与发布说明。

## 三、功能需求（详细）
### 3.1 依赖识别类型（默认全部支持，可通过配置开关）
1. **文件/头文件 include 依赖**
    - 分析源文件与头文件之间的 `#include` 关系。
    - 区分本地引用与系统引用；解析包含路径搜索顺序。
    - 支持 `#include` 宏展开后的真实包含关系（在有 `compile_commands` 时）。
2. **函数调用图（Call Graph）**
    - 建立「谁调用谁」以及「被谁调用」的有向调用关系。
    - 支持函数重载、模板函数实例化、成员函数、虚函数/override 的合理处理（精度视解析方式分级）。
3. **类继承 / 类型依赖**
    - 分析类/结构体的继承层次（单继承、多重继承、虚继承）。
    - 主要类型之间的组合/引用依赖（成员类型、模板类型依赖等）。
4. **符号引用 / 交叉引用**
    - 定位全局/静态变量、宏、函数、类型等的定义与所有使用位置。
    - 支持跨文件、跨模块跳转。
5. **链接 / 构建依赖（Link/Build Dependency）**
    - 分析编译/链接层面模块与目标（target，`.o`/`.a`/`.so`/`.lib`/`.dll`、可执行文件）之间的链接关系及其依赖来源。
    - 在有构建系统（CMake/Bazel/Qt 等）元数据或可推导时，识别「哪个 target 链接/依赖了哪个库或模块」。
    - 无构建元数据时基于 include/产物推断近似链接关系，并在 UI 标注精度来源。

> 解析精度分级设计：当使用 `compile_commands` + Clang 时提供「精确」语义级结果；fallback 到 tree‑sitter 时提供「近似」词法/结构级结果，并在 UI 中标注精度来源，避免误导用户。上述 5 类依赖默认全部启用，均可通过配置开关单独关闭。

### 3.2 输入与项目发现
- 优先自动发现 `compile_commands.json`（用于 Clang 精确解析）。
- 若无编译数据库：
    - 允许用户通过配置指定编译命令 / include 路径 / 宏定义。
    - 自动 fallback 到 tree‑sitter 轻量解析。
- 允许用户手动指定项目根目录、源文件/头文件包含目录，并支持 `includes` / `excludes` 过滤配置。
- 支持常见构建系统（CMake、Makefile、Ninja、qmake、Bazel、Xcode）生成 compile_commands 的引导说明。

### 3.3 可视化（Webview 交互依赖图）
使用 VS Code Webview 实现，具备以下能力（全部要求）：
1. **力导向图（Force‑directed graph）**
    - 节点可在画布内拖动，支持缩放与平移。
    - 不同依赖类型用不同节点样式/颜色与连线类型区分（文件/函数/类/符号）。
    - 悬停节点显示详情（类型、文件路径、行号、说明）。
2. **点击跳转与悬停**
    - 点击图中节点：跳转到对应源码定义位置（打开文件并定位到行）。
    - 悬停：显示摘要、统计、相关依赖。
3. **多视图联动（图 + 列表/树混合）**
    - 提供三种视图并联动：依赖图、层级树/列表（按目录或依赖方向）、表格（列：节点、依赖数量、文件、精度等级）。
    - 在某一视图操作/选中时，其他视图同步高亮与导航。
4. **导出能力**
    - 将依赖图导出为 PNG / SVG 图片。
    - 将依赖数据导出为 JSON（兼容常见图格式）便于后续处理。
5. **增量展开 / 聚焦子图**
    - 默认聚焦当前选中文件/符号，向外展示 k 层依赖（可配置层级）。
    - 支持交互式展开/折叠某个节点的下一层依赖，避免一次绘制全图导致卡顿。
6. **方向性**
    - 明确区分并可用开关切换「依赖方向」：上级是谁（被谁引用/调用）vs 下级是谁（引用了谁/调用了谁），以及双向。
7. **性能渲染**
    - 对超大图采用**节点聚类/折叠、虚拟化、后台绘制、必要时 LOD（按层级简化显示）**策略，保证交互流畅。

### 3.4 索引与管理（性能核心）
- **百万级 LOC 支持**：采用多线程并行解析，进程内/磁盘缓存，内存可控。
- **自动增量索引**：监听文件保存/变更，只重建受影响部分的符号表与子图。
- **磁盘缓存**：索引与图数据缓存到 `.vscode` /用户目录下的缓存目录，二次启动快速加载（可用文件 mtime/hash 做失效判定）。
- **索引任务视图**：显示当前索引进度、队列、耗时；支持后台运行不阻塞编辑器。
- **手动全量重建**：提供「重新索引全部」「清理缓存」命令。
- **增量解析示意图（自动进行）**
```
文件保存/变更
↓
确定受影响源文件/头文件 → 更新关联的 include/调用/符号
↓
局部重建受影响子图 → 合并回主图 → 缓存更新 → 通知 UI 刷新
```

### 3.5 数据模型
- 统一的节点类型（文件，函数，类/struct，变量，宏，枚举、target/库等）。
- 边类型（includes、calls、class/type dependency、symbol usage、links 等），每条边带方向、精度等级、源位置。
- 支持按类型、按精度、按模块筛选过滤。

## 四、功能以外的工程要求
- 工程结构清晰，分目录组织：`src/`（TS 插件）、`engine/`（C++ 引擎）、`webview/`（前端）、`scripts/`（构建与打包脚本）。
- 提供 `README.md`：安装、使用方法、编译构建指南、平台说明。
- 提供 `package.json` 完整配置：`activationEvents`、`commands`、`menus`、`configuration`（可配置项）、build 与打包脚本。
- 代码需规范、有必要的注释（重点难点处）、支持调试（VS Code 调试配置 `launch.json`）。
- 明确待办/已知限制（如 fallback 精度、对大文件首次索引耗时等）。

## 五、验收用例（可逐条自测）
1. 打开一个大型 C++ 项目目录 → 插件自动发现 `compile_commands` → 后台索引起始，用户可查进度。
2. 选中某个 `.cpp` 文件右键「查看依赖图」→ 弹出 Webview，默认显示该文件 k 层依赖（含被谁 include、调用/被调用、继承、链接关系）。
3. 点击调用方/被依赖方节点 → 跳转到对应源码定义位置。
4. 在图 / 树 / 表格间切换，选中一处其余视图联动高亮。
5. 修改某文件保存 → 依赖图自动局部更新。
6. 「导出 PNG/SVG/JSON」成功生成文件。
7. 打开无 `compile_commands` 项目 → fallback 模式无需配置即可出图，UI 标注精度。

## 六、输出交付物清单
请生产一个完整可运行的 VS Code 插件工程，包含：
- 完整的 TypeScript 插件源码（命令、配置、Webview、IPC、缓存、增量调度）。
- 完整的 C++ 引擎源码与 CMake 构建脚本（Clang 解析 + tree‑sitter fallback + 并行索引 + 图构建）。
- Webview 前端源码（力导向图渲染、树/列表、联动、导出）。
- 三平台的构建脚本与引擎二进制分发方案。
- `package.json`、`tsconfig.json`、`launch.json`、`.vscodeignore`、`.gitignore` 等配置文件。
- `README.md`（使用与构建说明）。

### 6.1 教学文档（DepScaner 使用与学习教程）
项目完成后，除工程代码外还必须提供一套面向使用者的教学文档，帮助开发者上手并理解如何借助 DepScaner 学习项目。文档统一放在 `docs/` 目录，建议包含以下内容（可梳理为多个 `.md` 文件）：
1. **docs/01‑快速开始.md（Getting Started）**
    - 安装 DepScaner（从 vsix /市场 /本地编译）。
    - 打开一个 C++ 项目、准备 compile_commands、触发首次索引的完整步骤（含截图/示意图占位）。
    - 三平台下引擎二进制准备说明。
2. **docs/02‑界面与操作指南.md（Features）**
    - 各命令、配置项、状态栏/菜单入口说明。
    - 三种视图（依赖图 /树 /表格）与联动、跳转、悬停、层级展开、导出的操作手册。
    - 索引视图与缓存/重建操作。
3. **docs/03‑如何用 DepScaner 学习项目.md（学习场景教程）**
    - 结合真实场景讲解：如何从入口函数调用图梳理模块调用链、如何通过 include 依赖定位模块边界、如何用类继承图理解架构、如何用交叉引用找到实现。
    - 分步骤的可操作示例（如「从 main() 出发找数据处理核心逻辑」）。
4. **docs/04‑解析与精度说明.md（Advanced）**
    - Clang（compile_commands）精确解析与 tree‑sitter fallback 的区别、适用场景、如何为项目配置以获得更精确结果。
    - 五种依赖类型的语义说明与限制。
    - 已知限制与常见问题（FAQ）。
5. **docs/05‑性能与配置参考.md（Reference）**
    - 全部可配置项字典、默认值、示例。
    - 万行级性能调优建议（层级、过滤、缓存目录等）。
6. **docs/CHANGELOG.md**：版本记录。

> 每篇文档语言为中文，步骤清晰、配有示例；同一份在 `docs/` 内维护，并在主 README 中以目录链接方式索引。

## 七、评审与验收标准
- [ ] 三平台可编译、可打包（vsix），无需用户本机安装编译器即可运行。
- [ ] 第五章 7 条验收用例全部通过。
- [ ] 5 类依赖（include / call / 类型 /符号 /链接）均实现且可在 UI 区分精度。
- [ ] 增量索引在保存后局部更新；百万级 LOC 性能说明与压力测试结果。
- [ ] 全工程统一命名为 DepScaner；UI 中英双语。
- [ ] `docs/` 教学文档齐全（见 6.1），README 可索引。

## 八、执行策略：一次性地自我更正并完整落地（首要指令）
受邀后须不中断、自主地推进直至整个项目完成，而非产出零散片段或等待逐条指令。

### 8.1 执行流程（自主推进，无需逐步批准）
1. 规划→建骨架→引擎核心（Clang+tree‑sitter、5 类依赖图、并行与增量索引）→插件集成（命令/配置/IPC/缓存/增量调度）→Webview 可视化→编译运行自证（引擎与 esbuild 打包、F5 实装自测、修运行期错误）→对照验收→收尾交付（README、构建脚本、docs）。

### 8.2 自我更正规则
- 以运行结果为准：未经验证不视为完成；报错即定位修复再前进，不跳步、不粉饰。
- 一致优先：引擎协议、IPC、UI 数据字段、打包路径保持一致，变动时同步全链。

### 8.3 完成判定
持续工作至：第五章用例全部通过、第七章验收全部满足、编译运行无错误、交付物完整为止，并附各步骤简短验证记录。

### 8.4 输出开销约束（省 token 硬性要求）
- 动手前先给出「文件清单 +交付顺序」，不要输出多套备选方案的对比分析。
- 对重复结构（命令、配置项、视图、相似 edge 处理）模板复用，不得逐字重写同一模式。
- 注释仅写在真正的难点处；不输出思考过程、备选方案表、逐文件解说。
- 一次端到端实现并自测，不做「先搭框架等用户验收」的分段交付。

---

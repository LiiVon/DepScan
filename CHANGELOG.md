# 更新日志

完整版本记录见 [docs/CHANGELOG.md](docs/CHANGELOG.md)（每条决策都写了原因与代价）。

## 0.1.2

- **修：真实大项目上「1/5 发现文件」直接失败**
  （`No mapping for the Unicode character exists in the target multi-byte code page`）。
  路径里有 emoji / 日文 / 部分中文组合时，引擎按 Windows ANSI 代码页做路径转换会抛异常；
  现在引擎内嵌 UTF-8 活动代码页清单，并且**所有**路径转换显式走 UTF-8。
  顺带修掉「中文名文件被静默跳过」的隐患（不报错、但少文件）。
  新增回归测试 `npm run test:unicode`。
- **阅读路线：层视图 + 降噪**。层 = 从起点算的跳数，大层自动分页（一页看不完的先给摘要）；
  降噪会折叠「纯转发 / 一行小函数」，但**不丢信息**——被折叠的名字挂在最近保留的那一步上。
- **库项目也能起头**：没有 `main` 时列出「导出接口候选」（程序入口 → 公开接口 → 调用图上的根），
  每行给出判据，起点由你点。
- **公开面标注**：声明在 `include/` 这类目录下的符号会被标出来（依赖图角标 + 阅读路线接口图标）。
- **架构边界检查**：公开头文件引用内部实现、目录之间互相 include 成环 —— 两条都**可证明**
  （不靠目录名猜层次），报到「问题」面板，能点进源码那一行；
  CI 出口 `depscan-core --once --violations`（有违规时退出码 1）。
- **图标换成煎蛋**，而且是代码画的：`128×128` PNG + `24×24` 单色 SVG 共用同一套几何。
- 其他修复：界面语言是中文时侧边栏视图标题却是英文；阅读路线现在把当前策略写在标题右侧
  （`广度优先 · 函数级 · 已降噪 · 层视图`）；符号位置不再停在声明 / 调用点。

## 0.1.0

首个可用版本：五类依赖识别（include / 调用 / 继承与类型 / 符号引用 / 链接）、
Webview 三视图联动可视化、增量索引与磁盘缓存、中英双语 UI、跨三平台引擎分发。
